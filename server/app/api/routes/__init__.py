"""
Route modules live here.

Each file should define a router for a small area:
- projects (uploads + metadata)
- testplan (mapper output)
- runs (start/stop/status)
- results (read test results)
- sse (server-sent events stream)
"""

