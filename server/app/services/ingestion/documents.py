"""
Document ingestion service.

Will contain:
- routines to accept stored upload references (not raw bytes)
- extract/normalize text from business requirements + swagger spec
- persist ProjectDocument metadata to DB
"""

